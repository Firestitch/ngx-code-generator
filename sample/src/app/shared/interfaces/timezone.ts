export interface Timezone {
  city: string;
  country_code: string;
  country_name: string;
  description: string;
  dst: number;
  dst_offset: number;
  dst_offset_formatted: string;
  identifier: string;
  utc_offset: number;
  utc_offset_formatted: string;
  zone_code: string;
  zone_dst_code: string;
  zone_name: string;
  indentifier: string;
}
